<view class="item-container" bind:tap="onNavigateTo" data-target="{{target}}">
    <view class="item-body">
        <image class="item-cover {{status==0?'item-completed':'item-learning'}}" url="{{url}}" mode="aspectFill"></image>
    </view>
    <view class="item-footer">
        {{status==0?"回顾成长提升之路":"继续学习"}}
    </view>
</view>