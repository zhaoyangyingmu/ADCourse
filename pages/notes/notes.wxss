/* pages/notes/notes.wxss */
.note_body {
  display: flex;
  flex-direction: row;
  flex-wrap: nowrap;
  white-space: nowrap;
  width: 90vw;
  height: 90vh;
  margin: auto;
}

.note_item {
  display: inline-block;
}

.note_index {
  text-align: center;
  color: rgb(215,215,215);
}